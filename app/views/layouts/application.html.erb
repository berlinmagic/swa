<!DOCTYPE html>
<html>
<head>
  <title>Swa</title>
  <link href='http://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>


<div id="headline">
    <div class="container_24">
        <div class="grid_8">
            <h1>berlinmagic</h1>
        </div>
        <div class="grid_16">
            <% if current_user %>
                <input type="search" id="magic_search" />
            <% else %>
                <%= form_for(:user, :url => user_session_path, :html => {:class => "magic_login_form"}) do |f| %>
                  <%= f.text_field :email, :placeholder => "Deine E-Mail:" %>
                  <%= f.password_field :password, :placeholder => "Dein Passwort:" %>
                  <%#= f.check_box :remember_me %>
                  <%= f.submit 'Login' %>
                  <div class="clearfix"></div>
                <% end %>
            <% end %>
        </div>
        
    </div>
</div>

    <%= yield %>





</body>
</html>
